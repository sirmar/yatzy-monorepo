name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: ['**']

jobs:
  ci:
    uses: sirmar/dev/.github/workflows/reusable-ci.yml@main
    with:
      mode: mdev
    secrets: inherit
