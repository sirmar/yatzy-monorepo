name: Shared CI

on:
  push:
    branches: ['**']
    paths:
      - 'shared/**'
      - '.github/workflows/shared.yml'
  pull_request:
    branches: ['**']
    paths:
      - 'shared/**'
      - '.github/workflows/shared.yml'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: shared
          target: dev
          push: true
          tags: ghcr.io/${{ github.repository }}:yatzy-shared-shared-${{ github.sha }}
          cache-from: type=gha,scope=yatzy-shared-shared
          cache-to: type=gha,mode=max,scope=yatzy-shared-shared

  check:
    needs: build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [lint, types]
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/pull-image
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          images: yatzy-shared-shared
      - run: make -C shared ${{ matrix.target }}

  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/pull-image
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          images: yatzy-shared-shared
      - run: make -C shared coverage
      - uses: actions/upload-artifact@v4
        with:
          name: coverage
          path: shared/.coverage
          include-hidden-files: true

  coverage:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.14'
      - run: pip install coverage
      - uses: actions/download-artifact@v4
        with:
          name: coverage
          path: shared/coverage
      - run: |
          cd shared
          coverage combine coverage/.coverage
          coverage report --show-missing
      - name: Write coverage summary
        run: |
          echo '## Coverage' >> $GITHUB_STEP_SUMMARY
          cd shared && coverage report --format=markdown >> $GITHUB_STEP_SUMMARY
