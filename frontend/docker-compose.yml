services:
  frontend:
    container_name: ${DEV_CONTAINER}
    image: ${DEV_IMAGE}
    ports:
      - '${DEV_PORT}:5173'
    environment:
      - API_PROXY_TARGET=http://backend:8000
      - AUTH_PROXY_TARGET=http://auth:8000
